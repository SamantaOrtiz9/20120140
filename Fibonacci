(defun fibonacci (n)
  (cond
    ((= n 0) 0)
    ((= n 1) 1)
    (t (+ (fibonacci (- n 1))
          (fibonacci (- n 2))))))

;; Example usage:
;; (fibonacci 0) ; Returns 0
;; (fibonacci 1) ; Returns 1
;; (fibonacci 5) ; Returns 5
